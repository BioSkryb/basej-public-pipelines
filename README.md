# basej-public-pipelines
Public-ready BaseJumper Nextflow pipelines. Each pipeline lives in its own subdirectory (e.g. basej-dnaqc/). Currently PRIVATE while pipelines are vetted; flip to public manually after review.
